sudo: required
dist: trusty
language: none

before_script:
  - sudo ./config/travis_setup.sh
  - ./toolset/run-tests.py --install client --install-only
  - ./toolset/run-tests.py --install database --install-only

script:
  - time ./toolset/run-ci.py verify "$TESTDIR"
