[package]
name = "chopin-benchmark"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "TechEmpower Framework Benchmarks — Chopin"

[[bin]]
name = "chopin-benchmark"
path = "src/main.rs"

[dependencies]
chopin-core = "=0.5.18"
chopin-pg = "=0.5.18"
kowito-json = "0.2.10"
nanorand = { version = "0.7", default-features = false, features = ["std", "wyrand"] }
