server-name: "h2o"
user: www-data
max-connections: 32768

listen:
  port: 8080

hosts:
  default:
    paths:
      /:
        file.dir: /

file.custom-handler:                  # handle PHP scripts
  extension: .php
  fastcgi.connect:
    port: /var/run/php/php7.3-fpm.sock
    type: unix
