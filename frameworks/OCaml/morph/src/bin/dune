(executable
 (name tfb)
 (public_name tfb)
 (libraries caqti caqti-lwt caqti-driver-postgresql lwt logs logs.fmt fmt.tty morph
   archi archi-lwt routes yojson ptime ptime.clock ptime.clock.os))

(env
 (docker
  (flags
   (:standard -ccopt -static))))
